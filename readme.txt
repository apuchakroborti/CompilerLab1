command to run the file:
i)lex mylexer.l
ii)gcc lex.yy.c -lfl
iii)./a.out
iV)gedit output.out


Description of input and output file:
The lexer program read the input file and then write the output after executing the program .So we need not put any input in the terminal.
If there is a need to change the input format, edit the "input.in" file and save then run the above command mentioned.
Then if the "output.out" file is opened then the output of the program  can be  seen . 



